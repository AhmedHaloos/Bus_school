<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_margin="4dp"
    android:background="?android:attr/selectableItemBackground"
    android:elevation="3dp"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/management_driver_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="4dp"
        android:text="trip number"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textDirection="rtl"
        android:textSize="18dp"
        android:textColor="@color/black"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/management_driver_phone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="4dp"
        android:text="trip date"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textDirection="rtl"
        android:textSize="14dp"
        android:textColor="@color/list_details_color"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/management_driver_name" />

</androidx.constraintlayout.widget.ConstraintLayout>